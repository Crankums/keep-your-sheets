<%# list of characters %>
<%# <%@characters.each do%>
<% if !current_user.characters.empty?%>
    <% current_user.characters.each do |char|%>
        <a href='/stats/<%=char.id%>'><%=char.char_name%></a><br/><br/>
    <% end %>
    <% else %>
        <p>No characters yet! Create new ones below!</p>
<%end%>
<%# link to create new character %>
    <a href='/characters/new'>Create a new character</a>
<%# link to profile %>
    <a href='/users'>